package problem894

import TreeNode
import sun.reflect.generics.tree.Tree

class Solution {

	val solutions = mutableMapOf<Int, TreeNode?>()

	fun allPossibleFBT(n: Int): List<TreeNode?> {
		val result = mutableListOf<TreeNode>()
		if (n % 2 == 0) return result

		for (i in 1 until n) {

		}
	}

	fun find(n: Int): TreeNode? {

	}
}